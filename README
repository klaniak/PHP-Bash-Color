use this example.php with a color ready terminal:

<?php

require_once 'bashcolor.php';

echo bash_color('hello world', 'blue', 'black');

?>

then go into cli, type "php example.php" and have some nice colors in your terminal :).
